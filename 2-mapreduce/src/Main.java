import java.io.IOException;

// Uncomment each job's import and call as you implement it. Enable both jobs
// before submitting the assignment.
//import jobs.WordCount;
//import jobs.ReverseLinks;

/** Runs the MapReduce jobs written for the assignment. */
public final class Main {

    public static void main(String[] args) throws IOException {
        // WordCount.run();
        // ReverseLinks.run();
    }
}
