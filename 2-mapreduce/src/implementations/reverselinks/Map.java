package implementations.reverselinks;

// The class name conflicts with java.util.Map, so no import is needed here.

/**
 * Implement the map stage for reverse investment links.
 *
 * Implement {@code MapperInterface}. The input types are fixed by
 * {@code jobs.ReverseLinks}, which has already split each line: every call
 * receives {@code (K1, V1) = (String, List<String>)}, one investor and the
 * companies it invested in.
 *
 * Emit pairs that group each company with its investor. Choose the
 * intermediate types K2 and V2; the reducer must use the same ones, and its
 * result has type V2 as well. The README explains what that means for V2.
 */
public final class Map {

    // TODO: implement the mapping interface and its one method.
}
