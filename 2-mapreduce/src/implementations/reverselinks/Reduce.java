package implementations.reverselinks;

/**
 * Implement the reduce stage for reverse investment links.
 *
 * Reduce all values for one company to one value holding every investor in
 * it, each investor once. The output file prints a collection as its elements
 * in ascending order, separated by single spaces. The input order is
 * unspecified, and {@code test/Invariants.java} checks that it does not affect
 * the result. The reducer should support partial reductions.
 */
public final class Reduce {

    // TODO: implement the reducing interface.
}
